// Note that this has to be manually updated to match the value in the configurations.
export const configuration = {
  LOCAL_STORAGE_FIRST_TIME_VISITOR_KEY: 'skraafoto-release-december-2024',
  /** Must match COOKIE_CONSENT_KEY / COOKIE_CONSENT_REJECTED in src/modules/tracking.js */
  COOKIE_CONSENT_KEY: 'skraafoto-cookie-consent',
  COOKIE_CONSENT_REJECTED: 'rejected',
}
