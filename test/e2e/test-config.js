// Note that this has to be manually updated to match the value in 'src/modules/configuration'.
// We can't import it because it fails due to no global `config` variable existing.
export const configuration = {
  LOCAL_STORAGE_FIRST_TIME_VISITOR_KEY: 'skraafoto-first-time-visit'
}
